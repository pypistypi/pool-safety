<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Разрешений ровно столько, сколько нужно для работы в локальной сети.
        Ни камеры, ни местоположения, ни памяти телефона приложение не просит:
        просить лишнее у программы, которую ставят на рабочий телефон охраны,
        значит объяснять потом, зачем это нужно, — а нужно оно не будет.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Картинка в картинке есть с Android 8.0, но обязательной не считаем:
         на телефоне, где её нет, приложение должно работать без неё. -->
    <uses-feature
        android:name="android.software.picture_in_picture"
        android:required="false" />

    <application
        android:name=".WatchApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="false"
        android:theme="@style/Theme.PoolSafety"
        android:usesCleartextTraffic="true">

        <!--
            usesCleartextTraffic нужен по существу: камеры отдают поток по
            http:// и rtsp://, шифрования у них нет и не будет. Сеть при этом
            локальная и наружу не выходит.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden"
            android:screenOrientation="fullUser">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.PoolSafety.Settings" />

        <service
            android:name=".service.WatchService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Наблюдение возвращается само после перезагрузки телефона и после
             обновления приложения. Иначе оно оказалось бы выключенным, и
             никто бы об этом не узнал. -->
        <!-- Кнопки «Принять» и «Заглушить» прямо в уведомлении о тревоге. -->
        <receiver
            android:name=".notify.AlarmActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
